# Internal / never publish to GitHub or the live site
.firecrawl/
*.md
supabase-schema.sql

# Superseded / dead files (old dark-theme system, not used by any page)
styles.css
app.js

# Editor cruft
vercel.json
Thumbs.db
.DS_Store
desktop.ini

# Secrets / credentials — never commit
.env
.env.*
*.pem
*.key
*.p12

# Local preview tooling
.claude/

# Wrangler (Cloudflare Worker) local cache/state
analytics-worker/.wrangler/
